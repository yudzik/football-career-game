import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yudzik.footballcareer',
  appName: 'Football Career',
  webDir: 'dist',
};

export default config;
